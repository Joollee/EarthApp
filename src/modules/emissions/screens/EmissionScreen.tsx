import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const EmissionScreen = () => {
  return (
    <View>
      <Text>EmissionScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
